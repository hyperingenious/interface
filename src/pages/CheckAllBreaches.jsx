import { Center, Input, Stack, Title } from "@mantine/core";

export default function CheckAllBreaches() {
  return (
    <Center p={"md"}>
      <Stack>
        <Title order={1}>Check All the Breaches</Title>
        <Input placeholder="Input component" />
      </Stack>
    </Center>
  );
}
